# Frontend environment template.
# Copy to `.env.local` (which is git-ignored):  cp .env.local.example .env.local
#
# NEXT_PUBLIC_ variables are exposed to browser code. Never put secrets here —
# anything NEXT_PUBLIC_ is visible to anyone using the site.

# Base URL of the backend API.
NEXT_PUBLIC_API_URL=http://localhost:8000
