/**
 * Next.js configuration.
 *
 * Kept intentionally minimal for the Phase 1 foundation. `reactStrictMode`
 * surfaces potential problems (e.g. unsafe lifecycles, double-invoked effects)
 * during development.
 *
 * @type {import('next').NextConfig}
 */
const nextConfig = {
  reactStrictMode: true,
};

export default nextConfig;
