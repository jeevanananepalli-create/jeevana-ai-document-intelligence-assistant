"""Use cases — one unit per user-facing action.

Each use case is a small, single-responsibility class (e.g. RegisterUserUseCase)
that orchestrates a single workflow end to end. Keeping one class per action
makes the system easy to read, test, and extend. Added in later phases.
"""
