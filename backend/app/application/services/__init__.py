"""Application services — reusable logic shared across multiple use cases.

When two use cases need the same piece of orchestration, it is extracted here
to avoid duplication. Added in later phases.
"""
