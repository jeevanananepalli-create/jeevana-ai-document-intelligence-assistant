"""Entities — objects defined by a unique identity that persists over time.

A User is the same user even if their name changes, because identity is the
`id`, not the attributes. Entities are added in later phases (no business
tables are created in Phase 1).
"""
