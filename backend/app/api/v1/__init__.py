"""API version 1 routes.

All v1 endpoints are mounted under the `/api/v1` prefix. Grouping by version in
the URL means a future breaking change can ship as `/api/v2` while `/api/v1`
keeps working for existing clients.
"""
