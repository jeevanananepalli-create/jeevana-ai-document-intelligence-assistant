"""External services — clients for third-party systems (OCR, LLM, storage).

These wrap outside dependencies behind domain interfaces so the rest of the app
never depends on a vendor SDK directly. Populated in later phases; intentionally
empty during the Phase 1 foundation.
"""
